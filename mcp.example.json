{
  "//": "MCP client config for Arbiter. For Claude Desktop, merge the 'arbiter'",
  "//2": "entry into claude_desktop_config.json. Set 'cwd' to this repo's absolute",
  "//3": "path and fill in your Gemini key. No GitHub App or database needed.",
  "mcpServers": {
    "arbiter": {
      "command": "python",
      "args": ["mcp_server.py"],
      "cwd": "/absolute/path/to/arbiter",
      "env": {
        "GEMINI_API_KEY": "your_gemini_api_key",
        "GEMINI_MODEL": "gemini-2.5-flash-lite"
      }
    }
  }
}
