# --- Web server / webhook ---
fastapi
uvicorn[standard]
python-dotenv

# --- GitHub App ---
PyGithub>=2.3.0
PyYAML

# --- Agent brain ---
langgraph
langchain-google-genai

# --- RAG: pgvector store + embeddings ---
# psycopg 3 (binary build avoids a local libpq) + connection pool + pgvector adapter.
psycopg[binary]>=3.1
psycopg-pool>=3.2
pgvector>=0.3

# --- MCP: expose the reviewer as tools to any MCP client (mcp_server.py) ---
mcp>=1.2

# --- Integrations: Linear (GraphQL over HTTP) ---
httpx
